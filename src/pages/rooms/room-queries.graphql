query getRoom($id: UUID!){
  roomById(id: $id) {
  	id
    name
    usersByRoomId {
      nodes {
        id
        name
        avatar
      }
    }
  }
}

query allRooms {
  allRooms {
    nodes {
      id
      name
      createdAt
      updatedAt
    }
  }
}
